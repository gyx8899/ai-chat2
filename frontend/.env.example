# Backend API URL (客户端浏览器调用)
NEXT_PUBLIC_API_URL=http://localhost:8080

# Backend Internal URL (服务端 RSC 调用后端，同机部署时用容器名/内网 IP)
BACKEND_INTERNAL_URL=http://localhost:8080

# 热更新密钥（与后端 REVALIDATE_SECRET / RELOAD_SECRET 一致，用于验证 reload webhook）
REVALIDATE_SECRET=your-secret-here